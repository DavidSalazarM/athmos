this project needs Python version 3.6.x

run the command "python main.py" to run the script with the default websocket url "ws://209.126.82.146:8080"

run the command "python main.py #{websocket_url}" to run the script with your specific websocket url

run the command "python -m unittest -v test.py " to run the tests